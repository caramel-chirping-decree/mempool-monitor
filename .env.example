# Mempool Monitor Configuration

# WebSocket endpoint (defaults to mempool.space mainnet)
MEMPOOL_WS_URL=wss://mempool.space/api/v1/ws

# Reconnect delay in seconds
RECONNECT_DELAY=5

# Webhook settings (configure in config.json for multiple webhooks)
# WEBHOOK_URL=https://your-webhook-url.com/endpoint
